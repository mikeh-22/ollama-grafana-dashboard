extends: default

rules:
  line-length:
    max: 160        # alert summaries and PromQL expressions can be verbose
  truthy:
    allowed-values: ["true", "false"]
  comments:
    min-spaces-from-content: 1
  document-start: disable   # provisioning files don't use ---
